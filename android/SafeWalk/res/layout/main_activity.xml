<?xml version="1.0" encoding="utf-8"?>
<android.support.v4.widget.DrawerLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/drawer_layout"
    android:layout_width="match_parent"
    android:layout_height="match_parent" >

    <!-- The main content view -->

    <RelativeLayout
        android:id="@+id/mainView"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_alignParentTop="true"
        android:animateLayoutChanges="true" >

        <FrameLayout
            android:layout_alignParentTop="true"
            android:id="@+id/fragmentContainer" 
            android:layout_width="match_parent"
            android:layout_height="500dip"
            android:animateLayoutChanges="true" >

            <fragment
                android:id="@+id/map"
                android:name="edu.purdue.SafeWalk.CustomMapFragment"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                class="edu.purdue.SafeWalk.CustomMapFragment" 
                android:animateLayoutChanges="true" />

            <include layout="@layout/map_popup_bubble" />
        </FrameLayout>

        <fragment
            android:id="@+id/callButtons"
            android:name="edu.purdue.SafeWalk.CallButtonFragment"
			android:layout_width="fill_parent"
        	android:layout_height="?android:attr/actionBarSize"
        	android:layout_alignParentBottom="true"
        	android:layout_below="@id/fragmentContainer"
            tools:layout="@layout/call_button_fragment" 
            android:animateLayoutChanges="true" />
    </RelativeLayout>
    <!-- The navigation drawer -->

    <ListView
        android:id="@+id/left_drawer"
        android:layout_width="220dp"
        android:layout_height="match_parent"
        android:layout_gravity="start"
        android:background="#ffffffff"
        android:choiceMode="singleChoice"
        android:divider="@android:color/black"
        android:dividerHeight="0dp" />

</android.support.v4.widget.DrawerLayout>